{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default Outlier desktop permissions: window controls + sidecar shell spawn.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:webview:default",
    "core:webview:allow-set-webview-zoom",
    "shell:allow-spawn",
    "shell:allow-kill",
    "updater:default",
    "fs:default",
    "dialog:default"
  ]
}
