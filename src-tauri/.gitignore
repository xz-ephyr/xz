# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Sidecar binaries are downloaded at build time (see scripts/prepare-sidecar.mjs).
# We don't want platform-specific binaries committed to the repo.
/binaries/
